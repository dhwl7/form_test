<!DOCTYPE html>
<html lang="en">
<head>
    <title>Profile page</title>
</head>
<body>
    <%- include ('common/header') %>
    <h1>Profile Page <%= user.name %></h1>
    <h3><%= user.email%></h3>
    <ul>
        <% user.skills.forEach((iteam) => { %>
            <li> <%= iteam %></li>
            <% }) %>    
    </ul>
</body>
</html>